import { IconType } from "react-icons/lib/esm/iconBase";

export interface CircleType {
    Icon: IconType,
    link: string,
    percentage: number;
    animate: boolean,
    isLink: boolean,
    text: string
}